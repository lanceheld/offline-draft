name: 'Auto Assign'
on:
  workflow_call:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  assign-author:
    name: Assign PR author
    runs-on: ubuntu-latest
    steps:
      - uses: toshimaru/auto-author-assign@v3.0.3
